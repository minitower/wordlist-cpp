#ifndef NUMBERGENERATOR_H_INCLUDED
#define NUMBERGENERATOR_H_INCLUDED

#include <vector>
#include <string>
using namespace std;

class NumberGenerator{
public:
    static vector<string> nGenerator(int length);
    static long long permutation(int num);
};
#endif // NUMBERGENERATOR_H_INCLUDED
